class Card {
	constructor(cardDetails) {
		this.suit = cardDetails.suit;
		this.range = cardDetails.range;
		this.color = cardDetails.color;
		this.unicode = cardDetails.unicode;
	}
}

export default Card;
